import { SummaryInfo } from '@/components/Forms/SummaryInfo/SummaryInfo';
import styles from '../styles/page.module.css';

const Summary = () => {
  return (
    <div className={styles.main}>
      <SummaryInfo />
    </div>
  );
};

export default Summary;
